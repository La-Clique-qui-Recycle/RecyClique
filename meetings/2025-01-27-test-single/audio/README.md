# Test avec fichier unique (le plus petit)

Copiez ici le fichier **"Reu recyclique 3.m4a"** depuis le dossier principal.

Une fois copié, lancez :
```bash
python scripts/aai_transcribe.py --meeting-id "2025-01-27-test-single" --verbose --concurrency 1
```
